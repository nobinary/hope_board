import React from 'react'

function Modal(props) {

    // mapInputElements = () => {

    // }

    return (
      <div>
          <p>This is a modal for {props.type}</p>
          {/* {INPUTLIST} */}
      </div>
    );
  }
  
  export default Modal;